"""
Stage 2: Group Assembly.

Job: Solve bounded subset-sum over candidate pools to handle 1:many and many:1
match groups (the 5.6% of the benchmark currently forfeited as single allocations).
"""


def assemble_groups(candidates, b, max_subset_size=8):
    """Placeholder for Stage 2 group assembly (bounded subset-sum)."""
    raise NotImplementedError("Stage 2 group assembly scheduled for future milestone.")
